name: Build luci-app-xray

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'true'
        
env:
  SDK_URL: https://downloads.openwrt.org/releases/19.07.3/targets/ramips/mt7621/openwrt-sdk-19.07.3-ramips-mt7621_gcc-7.5.0_musl.Linux-x86_64.tar.xz

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@main
      
    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo -E apt -qq update
        sudo -E apt -qq upgrade -y
        sudo -E apt -qq install -y git-core build-essential libssl-dev libncurses5-dev unzip gawk asciidoc binutils bzip2 gettext git libz-dev patch python3.5 unzip subversion mercurial ccache sudo rsync screen
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir
        
    - name: Download SDK
      working-directory: /workdir
      run: |
        wget $SDK_URL -O sdk.tar.xz
        tar -xf sdk.tar.xz
        
    - name: SSH connection to Actions
      uses: P3TERX/ssh2actions@v1.0.0
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh  != 'false') || contains(github.event.action, 'ssh')
        
